# 参数加载规则

每个类都通过其config结构体加载配置，config结构体按类间的组合/平行关系进一步包装，整合出唯一一个configs结构体，在主函数中通过命令行参数传递配置文件路径，使用reflect-cpp从yaml文件中反射构造config结构体用于初始化。

- 由于一个进程共享一个logger,所以LogLevel是放在最外层结构体的
- 嵌套的结构体成员命名：内部结构体对应的类名+conf，使用蛇形命名法